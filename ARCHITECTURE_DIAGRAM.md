# User Settings Architecture

## System Architecture Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│                         FRONTEND (Renderer Process)              │
│                                                                   │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │                    index.html / UI                        │  │
│  │                                                            │  │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐   │  │
│  │  │   Settings   │  │  Sync Page   │  │  Dashboard   │   │  │
│  │  │     Tab      │  │              │  │              │   │  │
│  │  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘   │  │
│  │         │                  │                  │           │  │
│  │         └──────────────────┴──────────────────┘           │  │
│  │                            │                               │  │
│  │                    window.electron.invoke()               │  │
│  └────────────────────────────┼───────────────────────────────┘  │
│                                │                                  │
└────────────────────────────────┼──────────────────────────────────┘
                                 │
                                 │ IPC Communication
                                 │
┌────────────────────────────────▼──────────────────────────────────┐
│                      BACKEND (Main Process)                        │
│                                                                     │
│  ┌──────────────────────────────────────────────────────────────┐ │
│  │                      main.js (IPC Handlers)                   │ │
│  │                                                                │ │
│  │  ipcMain.handle('get-user-settings')      ──┐                │ │
│  │  ipcMain.handle('get-user-setting')       ──┤                │ │
│  │  ipcMain.handle('set-user-setting')       ──┤                │ │
│  │  ipcMain.handle('update-user-settings')   ──┤                │ │
│  │  ipcMain.handle('reset-user-settings')    ──┤                │ │
│  │                                              │                │ │
│  └──────────────────────────────────────────────┼────────────────┘ │
│                                                  │                  │
│                                                  ▼                  │
│  ┌──────────────────────────────────────────────────────────────┐ │
│  │                   userSettings.js (Core Module)               │ │
│  │                                                                │ │
│  │  ┌────────────────────────────────────────────────────────┐  │ │
│  │  │  Class: UserSettings                                    │  │ │
│  │  │                                                          │  │ │
│  │  │  Methods:                                                │  │ │
│  │  │  • get(key)                                              │  │ │
│  │  │  • set(key, value)                                       │  │ │
│  │  │  • getAll()                                              │  │ │
│  │  │  • updateMultiple(settings)                              │  │ │
│  │  │  • resetToDefaults()                                     │  │ │
│  │  │  • shouldSyncAllCalendars()                              │  │ │
│  │  │  • isBackgroundSyncEnabled()                             │  │ │
│  │  │  • setSyncAllCalendars(enabled)                          │  │ │
│  │  │  • setBackgroundSync(enabled)                            │  │ │
│  │  │                                                          │  │ │
│  │  └────────────────────────┬─────────────────────────────────┘  │ │
│  │                            │                                    │ │
│  └────────────────────────────┼────────────────────────────────────┘ │
│                                │                                      │
│                                ▼                                      │
│  ┌──────────────────────────────────────────────────────────────┐   │
│  │                    electron-store                             │   │
│  │                                                                │   │
│  │  Defaults:                                                     │   │
│  │  {                                                             │   │
│  │    sync_all_calendars: false,                                 │   │
│  │    background_sync_enabled: true                              │   │
│  │  }                                                             │   │
│  │                                                                │   │
│  └────────────────────────────┬───────────────────────────────────┘   │
│                                │                                       │
└────────────────────────────────┼───────────────────────────────────────┘
                                 │
                                 ▼
┌─────────────────────────────────────────────────────────────────┐
│                    PERSISTENT STORAGE                            │
│                                                                   │
│  📁 user-settings.json                                           │
│                                                                   │
│  Location:                                                        │
│  • Windows: %APPDATA%\synk-pro\user-settings.json               │
│  • macOS: ~/Library/Application Support/synk-pro/...            │
│  • Linux: ~/.config/synk-pro/user-settings.json                 │
│                                                                   │
│  Content:                                                         │
│  {                                                                │
│    "sync_all_calendars": false,                                  │
│    "background_sync_enabled": true                               │
│  }                                                                │
│                                                                   │
└───────────────────────────────────────────────────────────────────┘
```

## Data Flow Diagrams

### 1. Get Settings Flow

```
Frontend                    Backend                     Storage
   │                          │                           │
   │  invoke('get-user-      │                           │
   │  settings')              │                           │
   ├─────────────────────────>│                           │
   │                          │                           │
   │                          │  userSettings.getAll()    │
   │                          ├──────────────────────────>│
   │                          │                           │
   │                          │  { sync_all_calendars:    │
   │                          │    false, ... }           │
   │                          │<──────────────────────────┤
   │                          │                           │
   │  { success: true,        │                           │
   │    settings: {...} }     │                           │
   │<─────────────────────────┤                           │
   │                          │                           │
```

### 2. Set Settings Flow

```
Frontend                    Backend                     Storage
   │                          │                           │
   │  invoke('set-user-      │                           │
   │  setting', 'sync_all_   │                           │
   │  calendars', true)       │                           │
   ├─────────────────────────>│                           │
   │                          │                           │
   │                          │  userSettings.set(        │
   │                          │    'sync_all_calendars',  │
   │                          │    true                   │
   │                          │  )                        │
   │                          ├──────────────────────────>│
   │                          │                           │
   │                          │  ✓ Saved                  │
   │                          │<──────────────────────────┤
   │                          │                           │
   │  { success: true }       │                           │
   │<─────────────────────────┤                           │
   │                          │                           │
```

### 3. Integration with Sync Logic

```
Sync Manager                User Settings              Google Calendar
     │                           │                           │
     │  shouldSyncAll            │                           │
     │  Calendars()?             │                           │
     ├──────────────────────────>│                           │
     │                           │                           │
     │  true/false               │                           │
     │<──────────────────────────┤                           │
     │                           │                           │
     │                                                        │
     ├─── if true: fetch all calendars ──────────────────────>│
     │                                                        │
     │                           all calendars               │
     │<───────────────────────────────────────────────────────┤
     │                                                        │
     ├─── if false: use selected calendar only               │
     │                                                        │
```

## Component Relationships

```
┌─────────────────────────────────────────────────────────────┐
│                                                              │
│                    Application Components                    │
│                                                              │
│  ┌──────────────┐         ┌──────────────┐                 │
│  │              │         │              │                 │
│  │  Sync        │────────>│  User        │                 │
│  │  Manager     │  uses   │  Settings    │                 │
│  │              │         │              │                 │
│  └──────────────┘         └──────────────┘                 │
│                                   │                         │
│  ┌──────────────┐                 │                         │
│  │              │                 │                         │
│  │  Background  │────────────────>│                         │
│  │  Sync        │      uses       │                         │
│  │              │                 │                         │
│  └──────────────┘                 │                         │
│                                   │                         │
│  ┌──────────────┐                 │                         │
│  │              │                 │                         │
│  │  Calendar    │────────────────>│                         │
│  │  Filter      │      uses       │                         │
│  │              │                 │                         │
│  └──────────────┘                 │                         │
│                                   │                         │
│  ┌──────────────┐                 │                         │
│  │              │                 │                         │
│  │  Settings    │────────────────>│                         │
│  │  UI          │      updates    │                         │
│  │              │                 │                         │
│  └──────────────┘                 │                         │
│                                   │                         │
└───────────────────────────────────┼─────────────────────────┘
                                    │
                                    ▼
                          ┌──────────────────┐
                          │                  │
                          │  electron-store  │
                          │                  │
                          └──────────────────┘
```

## Settings Schema

```
UserSettings
├── sync_all_calendars: boolean (default: false)
│   ├── Purpose: Control calendar sync scope
│   ├── Values: true = all calendars, false = selected only
│   └── Used by: Sync Manager, Calendar Filter
│
└── background_sync_enabled: boolean (default: true)
    ├── Purpose: Control background sync behavior
    ├── Values: true = auto sync, false = manual only
    └── Used by: Background Sync, Sync Manager
```

## File Structure

```
synk-fixed/
│
├── src/
│   ├── main.js                    # IPC handlers (lines 754-810)
│   ├── userSettings.js            # Core settings module ⭐
│   ├── userSettingsExample.js     # Usage examples
│   ├── syncManager.js             # Will use settings
│   └── ...
│
├── test-user-settings.js          # Test script
├── IMPLEMENTATION_SUMMARY.md      # Complete summary
├── USER_SETTINGS_MIGRATION.md     # Migration guide
├── SQL_EQUIVALENT.md              # SQL reference
├── QUICK_REFERENCE.md             # Quick reference
├── ARCHITECTURE_DIAGRAM.md        # This file
└── ...
```

## Technology Stack

```
┌─────────────────────────────────────┐
│         Technology Stack             │
├─────────────────────────────────────┤
│                                      │
│  Frontend:                           │
│  • HTML/CSS/JavaScript               │
│  • Electron Renderer Process         │
│  • IPC Communication                 │
│                                      │
│  Backend:                            │
│  • Node.js                           │
│  • Electron Main Process             │
│  • electron-store (v8.2.0)           │
│                                      │
│  Storage:                            │
│  • JSON file-based                   │
│  • Persistent across sessions        │
│  • Atomic writes                     │
│                                      │
└─────────────────────────────────────┘
```

## Security & Data Flow

```
┌─────────────────────────────────────────────────────────┐
│                    Security Layers                       │
│                                                           │
│  1. IPC Isolation                                        │
│     • Frontend cannot directly access file system        │
│     • All storage operations through IPC handlers        │
│                                                           │
│  2. Type Validation                                      │
│     • Boolean validation for settings                    │
│     • Default values ensure valid state                  │
│                                                           │
│  3. Error Handling                                       │
│     • Try-catch blocks in all handlers                   │
│     • Graceful fallbacks to defaults                     │
│                                                           │
│  4. Data Persistence                                     │
│     • Atomic writes via electron-store                   │
│     • Automatic backup on corruption                     │
│                                                           │
└─────────────────────────────────────────────────────────┘
```

## Summary

**Architecture Type:** Client-side persistent storage with IPC communication

**Key Components:**
1. ✅ Frontend UI (Renderer Process)
2. ✅ IPC Handlers (Main Process)
3. ✅ Settings Module (Core Logic)
4. ✅ electron-store (Persistence Layer)
5. ✅ JSON File (Storage)

**Data Flow:** UI → IPC → Settings Module → electron-store → JSON File

**Integration Points:** Sync Manager, Background Sync, Calendar Filter, Settings UI

**Status:** ✅ Complete and Tested